Card Ledger 1.3.0 update patch

Copy these files into the root of your existing CardLedgerAndroid project and replace existing files when prompted.
This patch preserves your local.properties, Gradle wrapper JAR, and device keystore.
After copying, rebuild and install the app with adb install -r to keep local data.
